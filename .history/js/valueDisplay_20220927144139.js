let valueDisplay = document.querySelectorAll(".value");
let interval = 3000;
valueDisplay.forEach((valueDisplay) => {
  let startValue = 0;
  let endValue = parentInt(valueDisplay.getAttribute("data-val"));
  console.log(endValue);
});
